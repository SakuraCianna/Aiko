# GLM API 地址
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# 主模型名称
GLM_MODEL=glm-4.6v-flash
# 备用模型列表, 多个模型用逗号分隔
GLM_FALLBACK_MODELS=glm-4v-flash
# GLM API Key
GLM_API_KEY=replace-with-your-api-key

# 是否启用 Tavily MCP
MCP_TAVILY_ENABLED=false
# Tavily MCP 连接模式
MCP_TAVILY_MODE=stdio
# Tavily MCP stdio 包名
MCP_TAVILY_PACKAGE=tavily-mcp@0.2.19
# Tavily MCP remote 地址
MCP_TAVILY_REMOTE_URL=https://mcp.tavily.com/mcp/
# Tavily 每次搜索最大结果数
MCP_TAVILY_MAX_RESULTS=5
# Tavily 请求超时时间, 单位毫秒
MCP_TAVILY_TIMEOUT_MS=15000
# Tavily 单个 API Key
TAVILY_API_KEY=replace-with-your-tavily-api-key
# Tavily API Key 轮询列表, 多个 Key 用逗号分隔
TAVILY_API_KEYS=replace-with-key-1,replace-with-key-2,replace-with-key-3

# 是否启用本地 faster-whisper ASR
AIKO_ASR_ENABLED=false
# ASR provider, 当前固定为 faster-whisper
AIKO_ASR_PROVIDER=faster-whisper
# 本地 faster-whisper HTTP 服务地址
AIKO_ASR_BASE_URL=http://127.0.0.1:9001
# faster-whisper 识别语言
AIKO_ASR_LANGUAGE=zh
# ASR 请求超时时间, 单位毫秒
AIKO_ASR_TIMEOUT_MS=30000

# 是否启用本地 CosyVoice TTS
AIKO_TTS_ENABLED=false
# TTS provider, 当前固定为 cosyvoice
AIKO_TTS_PROVIDER=cosyvoice
# 本地 CosyVoice HTTP 服务地址
AIKO_TTS_BASE_URL=http://127.0.0.1:9002
# CosyVoice 默认音色名称
AIKO_TTS_VOICE=aiko
# TTS 音频输出格式
AIKO_TTS_FORMAT=wav
# TTS 请求超时时间, 单位毫秒
AIKO_TTS_TIMEOUT_MS=30000
